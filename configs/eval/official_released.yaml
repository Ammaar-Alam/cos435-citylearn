env:
  base_config: configs/env/citylearn_2023.yaml
  split: public_dev

evaluation:
  controller_ref: null
  eval_episodes: 1
  deterministic: true
  max_steps: null
  save_rollout_trace: false
  save_raw_kpis: true
  save_challenge_metrics: true
  export_simulation_data: false
  capture_render_frames: false
  max_render_frames: 0
  render_frame_width: 960

logging:
  metrics_root: results/metrics
  tables_root: results/tables
  manifests_root: results/manifests
