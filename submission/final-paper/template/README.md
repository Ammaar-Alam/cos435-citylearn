# Final report template

This folder preserves the course-provided final report LaTeX template as received.

- `_COS_435__Intro_RL_Final_Project.zip` is the original zip bundle.
- `source/` is the extracted template source.

The actual project report lives one level up as `final-report.tex` and `final-report.pdf`.
